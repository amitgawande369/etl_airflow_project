ETL Pipeline with Apache Airflow and SQLite/PostgreSQL Description: 
Build an ETL pipeline that extracts data from multiple sources (CSV, JSON, APIs), transforms it, and loads it into a relational database like PostgreSQL or SQLite. 
Use Apache Airflow to schedule and orchestrate the ETL workflow.
